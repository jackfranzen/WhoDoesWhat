# WhoDoesWhat — implementation reference

Detailed implementation notes for contributors. For the user-facing overview, see [README.md](README.md); for the message-by-message network reference, see [SYNCING.md](SYNCING.md); coding agents should start with [AGENTS.md](AGENTS.md).

This document records current design intent and historical client observations. It is not an API contract, and some client-specific notes can become stale as Anniversary builds change. Verify uncertain behavior against the current code and client.

Large WoW addon (TBC 2.5.5 Anniversary) for managing raid assignments, roles, and buffs.

## Tech Stack
**In use:** Lua 5.1 (WoW Classic API) · AceGUI-3.0 + custom `CreateFrame` chrome (WdwFrame.lua) + modern Menu API (right-click) · AceAddon/AceConsole/AceDB-3.0 · LibClassicInspector (+LibDetours) for talent sync — caches in-range/peer talents, we consume `TALENTS_READY`; can't fetch, so out-of-range non-users stay unknown · AceComm-3.0 + LibSerialize + LibDeflate (copied from the user's WeakAuras) for the assignment sync layer (Sync.lua) · LibCustomGlow-1.0 (copied from NovaConsumesHelper) for the buffing bar's per-button glow.

## Coding Guidelines
* **Never edit `Libs/`** — treat all bundled libs as read-only; customize via overlays/wrappers/subclassing, never patch.
* **Logging:** verbose dev logs via `WhoDoesWhat:Print(...)`.
* **Class/spec metadata:** always from `WhoDoesWhat.Classes` (Data.lua).

## Files
* `WhoDoesWhat.toc` — file manifest
* `Core.lua` — init, AceDB defaults, migrations
* `SlashCommands.lua` — slash router (/wdw)
* `Permissions.lua` — leader-owned editing-permission model (who may edit the board); see Features
* `Data.lua` — class/color/role/abilities metadata, buff defaults, customization storage
* `TalentScanning.lua` — LibClassicInspector consumer: role auto-detect, paladin buff-talent scan, kick/reinvite re-sync
* `BuffTracking.lua` — live active-blessing scanner for group members + hunter pets (see Features); `WhoDoesWhat:HasBuff` / `GetActiveBuffs`
* `Assignments.lua` — assignment MODEL (no frames), exported on `WhoDoesWhat.Assign`
* `AssignmentsActions.lua` — Get/Set API mapping unit-menu picks onto tank/CC/misdirect rows
* `FakeRaid.lua` — "Populate Fake Raid" testing toggle: injects 23 fake raiders (see Features)
* `Sync.lua` — network sync module (AceComm "WhoDoesWhat" prefix + LibSerialize/LibDeflate); see Features
* `PallyPowerBridge.lua` — push computed buff grid into PallyPower over its own wire protocol; PLPWR traffic log
* `WdwFrame.lua` — window chrome factory (backdrop, title, close, drag, Escape)
* `UnitMenuExtensions.lua` — unit right-click menu additional options, role/cc/MD setters
* `RaidFrameExtensions.lua` — spec icon drawn over the group icon on Blizzard's compact raid/party frames (hooks `CompactUnitFrame_UpdateRoleIcon`)
* `RaidMenuExtensions.lua` — main-tank promote helper extension for WoW raid view UI
* `Views/` — one file per UI panel:
  * `SectionKit.lua` — shared widget primitives for the assignment sections (`WhoDoesWhat.SectionKit`): geometry constants, box chrome with injectable right-aligned header buttons (`CreateSectionChrome` + `ChainHeaderButton` + `LayoutHeaderChain`), mail/warning/dropdown/row helpers, `AddPlayerMenuItems`, scroll-height bookkeeping, and the clear-all confirm popup. Section-agnostic — no flags, no per-section branches.
  * `Sections/` — the five assignment sections, **hard-coded one file each** as `Build(f, content)`/`Refresh(f)` pairs registered on `WhoDoesWhat.SectionViews`: `TankSection` (auto row per marked tank, multi-select markers, clear-all), `CCSection` (user-grown rows — **the template to copy for future sections** like healer/custom assignments; see its header comment), `MisdirectSection` (auto row per hunter, clear-all), `PaladinBuffsSection` (computed summary + local buff rules + no-paladin gray-out), `WarlockCursesSection` (fixed row per curse + Auto/Calc). Each owns its rows, warnings, read-only swap, header buttons and box height.
  * `MainAssignmentsView.lua` — main /wdw window: chrome, top buttons, permission strip, scroll/columns, and the refresh coordinator (`RefreshAll` = permission strip → mail visibility → each section's Refresh → mail enabled-states). Builds sections in fixed order: left (narrow) PaladinBuffs/WarlockCurses, right (wide) Tank/CC/Misdirect.
  * `MembersView.lua` — members bucketed by role; per-row group-role + WDW-role dropdowns, talents + Rescan, and a leading warning gutter listing everything wrong with that member (absorbed the former `ActionItemsView.lua`; the judging lives in `ActionItems.lua`)
  * `RaiderTooltipView.lua` — shared paladin/warlock utility tooltip for roster rows
  * `BuffingGridView.lua` — raid-buff status + raiders × paladins blessing grid
  * `PaladinBuffingBarView.lua` — the clickable Paladin Buffing Bar (see Features)
  * `WarriorShoutBarView.lua` — the Warrior Shout Bar (see Features)
  * `AboutView.lua` — About & Updates window: copyable links/contact details, installed version, and a static dated release-notes picker
  * `PallyPowerLogView.lua` — PallyPower traffic log window (/wdw pplog)
  * `PallyPowerDiffView.lua` — PallyPower Differences window (Paladin Buffs section "Check" button): formatted per-paladin drift report from `CheckPallyPowerSync`, with a per-paladin blessing-spread overview above each grid and Fix All / per-row Fix; it repaints itself from `RefreshBuffingGridView` and hides once nothing differs
  * `AllRolesView.lua` — "Role Preferences" landing page
  * `SingleRolesCustomizeView.lua` — customize a spec's buff priority + role
  * `AddonSettingsView.lua` — Developer Options
  * `CurseCalculatorView.lua` — Details!-backed curse value calculator

## Features
* **Role & buff data (Data.lua):** all TBC classes/specs (icons, colors), collapsed categories, per-role default paladin buff priorities and default bans (backfilled to a full 6-buff order plus an `allowedCount` divider), custom roles. DB stores only deviations; `GetEffectiveBuffSetup` feeds the editor and `GetEffectiveBuffOrder` returns only the allowed prefix for assignment math. `PaladinBuffs`/`CCSpells`/`WarlockCurses` use TBC max-rank spellIds with Classic Era overrides in `ClientFeatures.lua`; Classic also adds Curse of Shadow. Wrong ids surface as "?" (CC icons via `GetSpellIcon`). `RaidTargetMarkers` skull-first; chat tokens = lowercased marker names.
* **Hunter pets (`hunter_pets` pseudo-role):** NOT assignable or customizable — removed from the Hunter role/category lists (no Set Role menu, Members page dropdown, or Role Preferences page offers it; a one-off Core.lua migration clears old assignments + its customization). Registered by hand in `RolesAndCategories` (like Non-raider) so stale synced assignments still resolve. Instead every hunter automatically contributes one virtual pet (`GetPetMembers` in Assignments.lua, exported on `Assign`): `"<Hunter>'s Pet"` entries (apostrophe can't collide with character names; `owner`+`isPet` fields) that live only in the paladin-buff math — demand votes + own `ComputeBuffGrid` rows wanting `HunterPetBuffOrder = {might, kings, light}` from Data.lua (deliberately NOT backfilled, so a fully covered pet shows an empty cell). Non-raider hunters contribute no pet. Units that hold the pet slot without being a pet are skipped by every pet scan (`WhoDoesWhat:IsIgnoredPetName` in Core.lua, currently just the Steam Tonk Controller's "Steam Tonk"): `GetPetUnitInfo`, `BuffTracking.GroupTargets` and the bridge's `LivePetNames` all drop it, so the hunter reads exactly as if no pet were summoned — no name, no aura scan, no castable unit, no PallyPower target. In the buff grid the pets form their own section immediately after Warriors; they retain Hunter metadata/colors and use the pet role icon. Hunter pets ride the **Warrior blessing group** — a Greater Blessing on a Warrior reaches them (PallyPower files pets under Warrior for this reason). The shared plan matches each pet's wants across the **full** paladin pool like any other row, with a `PET_GREATER_BONUS` (200 — above any talent gap, far below one position step) for pairing a pet with the paladin whose Warrior Greater IS that blessing; cells that don't match the Warrior Greater become individual **Normal** blessings. So a shared Greater wins ties but never costs the pet a higher-priority blessing (a Kings-talented paladin holding the Warrior Might Greater gives the pet Kings while someone else covers Might). `PallyPowerBridge` and `GetPaladinBuffJobs` consume that same plan and Warrior Greater decision; unresolved real pet names and fake hunters' pets cannot become castable individual targets.
* **Buff tracking (BuffTracking.lua):** live scan of which paladin blessings and configured status auras are active on each group member **and hunter pet**, matched by localized aura name (rank-proof, Greater or Normal). Modern `C_UnitAuras.GetBuffDataByIndex` (indexed `UnitBuff` fallback), per-unit on `UNIT_AURA` + a 3s full-group backstop poll. In Classic the aura LIST is available at any range for group members; the last observed expiration is retained when duration goes stale out of range. Dead is a directly-scanned status, not an aura. Pets are keyed `"<Owner>'s Pet"` to match the plan. API: `WhoDoesWhat:HasBuff(name,key)` → true/false/nil, `GetActiveBuffs`, and `GetBuffTimeRemaining`. `RefreshBuffingGridView` is the shared repaint hook.
* **Paladin Buffing Bar (PaladinBuffingBarView.lua):** a movable, Nova-style **secure** bar — a clickable alternative to PallyPower that casts blessings directly (works with or without PallyPower installed). One button per CLASS the local paladin is assigned, from `GetPaladinBuffJobs` (on `Assign`), which collapses the active WDW/PallyPower plan into a class Greater + per-player Normal exceptions. PP source mode prefers a co-installed addon's live tables and falls back to WDW's observed wire mirror. **Left-click** casts the class Greater on a member (rotates through all resolvable members for range robustness); **right-click** cycles every assigned member's planned Lesser Blessing, including recipients of the class Greater, ordered missing first and then by shortest remaining duration. Secure `Execute`/`WrapScript` rotation (PallyPower's proven pattern, snippet branches on `button`) is baked out of combat and rebaked on `PLAYER_REGEN_ENABLED`; cast targets are unit tokens (`[@raid5]`/`[@raid5pet]`). Existing class buttons still repaint their effective job, `buffed/total` count, range state, and glow in combat; protected spell/target attributes and any added/removed class-button layout wait for `PLAYER_REGEN_ENABLED`. **Hunter pets bucket under the Warrior button** — a Greater on a Warrior reaches hunter pets, so a pet whose want matches the class Greater is covered for free; right-click can still top the pet up with its planned Lesser (real pet unit via `GetPetUnitInfo`). A pets-only bucket shows the pet icon, while a mixed one shows the class icon + a small pet badge. Per-button count comes from `HasBuff`, the icon greys when nothing's castable in range, and LibCustomGlow PixelGlow marks a reachable missing member. In PP source mode the grey gear button is a complaint — N classes with no PallyPower assignment for this paladin — so it only appears while that count is above zero (either orientation). `settings.buffingBarHideCompleted` (off) drops a class button while `JobIsDone` — `covered >= total` **and** no countdown running, since a warning is work and a class showing one is not finished whatever its count reads (without that, a class would hide on its way out and reappear seconds later with its blessing gone) — leaving only outstanding work; buttons return as a blessing runs into the warning window, but adding or removing one is a layout change and waits for `PLAYER_REGEN_ENABLED`, and a bar emptied that way says "All assigned blessings are up." rather than the no-assignments hint. **Expiry countdown**, the shout bar's trick applied per class: `GetPaladinBuffJobs` also returns `soonest` — the shortest `GetBuffTimeRemaining` among the members it already asked `HasBuff` about, so it costs nothing extra — which the view stores as an absolute `btn.expiresAt` and `UpdateJobTimer` counts down on the existing 0.5s tick with arithmetic alone, no aura rescans. It draws over the icon (13px outlined off `GameFontNormal`'s face — smaller than the shout bar's 18px because "10m" is three characters on a 28px button) in both orientations, and only inside the warning window. That window is `settings.buffingMenuWarnMinutes` (`WhoDoesWhat.BuffingWarnMinutes`, 10 down to 1, default 6, Settings > Paladin Bar), one threshold driving both tells: the countdown and the yellow player rows that used to be the fixed five-minute `buffingMenuWarnExpiring` checkbox. `FormatCountdown` prints whole minutes rounded UP above 60s and seconds at or below it, so a number always means "at least this much left" and "1m" never appears — 2m gives way straight to 60s. Alt-drag to move, from anywhere on the bar **including the buttons** (`AttachAltDrag` on every one of them, the shout bar's approach — the icons cover most of the bar, and a column leaves almost nothing else to grab). Alt therefore means "I am moving this", so both hover snippets bail on `IsAltKeyDown()` after closing the other popouts — a held Alt closes what is open and opens nothing — and `CloseAllPopouts` shuts the menus and tooltip when a drag actually starts, so nothing rides along on top of where you are placing the bar (combat-guarded: they hang off secure buttons). `settings.buffingBarOrientation` lays the buttons out as a row (HORIZONTAL) or a column (VERTICAL); everything directional follows that axis, and `PlaceButton`/`AnchorCount` are the only two places that know which way it runs — a row hangs each count under its button and pays for it once in the bar's height, while a column comes out exactly one icon wide (like the shout bar with numbers off): the class buttons' x/y counts drop out (coverage is still in the player menu's header) and the Righteous Fury countdown moves onto its icon, 13px outlined and centred, the way the shout bar's expiry countdown sits on a shout. Growth is RIGHT/LEFT/CENTER in a row and DOWN/UP/CENTER in a column, encoded as the saved anchor point (`GROW_POINTS[axis]` → `TOPLEFT`/`TOPRIGHT`/`TOP`, `TOPLEFT`/`BOTTOMLEFT`/`LEFT`) so a saved position is self-describing and a mode change — growth or orientation — re-derives it from the current rect; each half of the point says whether its coordinate is an edge or that axis's midpoint (`GetCenter`), and `ClampPosition` keeps both halves on screen. Turning the bar translates both grow settings through `GROW_FLIP`/`MENU_FLIP` (right↔down, left↔up), so flipping back restores the original choice; `BarGrow`/`MenuGrow` coerce anything left on the wrong axis. Popouts follow too: `PopoutDirection` picks UP/DOWN in a row and RIGHT/LEFT in a column (flipping to the roomier side when the preferred one won't fit), and `AnchorPopout`'s sideways case steps out to the bar's edge rather than the button's so a column's counts stay visible. The title strip reads `Paladin Bar` at the left with a separate grey `WDW`/`PP` source token a point smaller at the right end, and the bar's minimum width respects both string widths so they can't collide; a column is only one button wide, so it drops the name and centres the source token alone, a point down off the same face -- a full-size "WDW" was wider than a button and used to widen the whole bar around itself, which is why a WDW column looked fatter than a PP one; the token is sized to the strip now, not the strip to the token, so `SetSize` there is one flat icon width. Its tooltip carries one grey line naming the source ("Buffing data is powered by …"), then double-lines for Alt-Drag, Alt-Right-Click (Toggle PallyPower, followed by a blank line -- it acts on the bar itself, like the drag above it, rather than opening a window like the two below), Shift-Left-Click (Buffing Grid) and Shift-Right-Click (`OpenAddonSettingsView("Paladin Bar")`). **Standing down for PallyPower:** with PallyPower installed, Alt-Right-Click on the strip flips its master switch through `TogglePallyPower` (PallyPowerBridge.lua), which does exactly what PallyPower's own options setter does -- `opt.enabled` plus the matching `OnEnable`/`OnDisable` pass -- and refuses in combat, since that pass shows and hides PallyPower's secure buttons. That switch fires no event, so the bridge's OnEnable hooksecurefuncs both of those methods (anything flipping the switch, its options panel included, has to call one): without it our bar only noticed on the next repaint it happened to get, which solo could be the next time you dragged it. While `PallyPowerIsEnabled()`, `CollapseForPallyPower` empties our bar to its title strip alone (`INSET * 2 + TITLE_H` tall, wearing `COLLAPSED_EDGE` -- the tooltip border draws corners `edgeSize` across, so a frame shorter than two of them frays, and 16 on an 18px strip did -- exactly the strip, since CONTENT_TOP would leave the button row's 2px showing as a thin black line of window body under it, the words' width in a row, unchanged in a column so the strip doesn't jump) with the title in red and a red tooltip line saying why -- replacing the "Buffing data is powered by" line rather than joining it, since a bar drawing nothing has no source to report -- and the Alt-Right-Click line naming the RESULT of the press ("Enable WhoDoesWhat" green, or "Disable for PP" red when the bar is live) rather than the mechanism: PallyPower's bar is doing this job, and two rival sets of blessing buttons help nobody. It sits BELOW the combat branch, emptying the bar being a layout change like any other. Without PallyPower installed none of this exists -- no tooltip line, no click, no collapsed state. **Status Bars** moved to the same pair (`StatusBarsClick` gates on shift for both buttons instead of bare right-click), so the PallyPower row's plain-left-click Differences shortcut had to start ignoring shift-clicks. It now matches the Paladin Bar exactly: Shift-Right-Click opens its settings, and Alt-Right-Click is a ROW shortcut only (that row's cog options). What used to hold those -- a window-level Alt-Right-Click for settings, and Announce All on Shift-Right-Click -- are gone, the second along with the header percentage and its `includeInTotal` per-check option: reading every check out to the raid at once is a wall of text, and an overall coverage number nobody could act on was sitting where the window's name goes. `AnnounceRow` stays on Shift-Right-Click over a row, and Shift-Left-Click over one WHISPERS instead of opening the grid (`WhisperRow`): a paladin row reuses the Paladin Buffs section's own `CollectPaladinBuffWhispers` text so a paladin hears the same words whichever window nudged them -- and that text is now `CoverageSummary` too (`WhoDoesWhat:CoverageSummary` + `MAX_NAMED_MISSING` live in Core.lua for exactly this), names only, since a paladin knows their own assignment and the old "Target->Blessing" pairs turned one line into a paragraph, a `selfSupplied` check (food) tells everyone still missing it to "Check your Food Buff!", and everything else goes to `SuppliersForCheck` -- the required class, minus anyone the talent scan says cannot cast it at all, then only the highest improvement rank anybody actually carries, since asking a rank-0 priest to re-cast over rank-2 Fortitude asks for the wrong buff (unscanned sorts below every real rank, so it only wins when nothing is known; no talent at all leaves the class tied). Pets whisper their owner, fake raiders and yourself are dropped at the whisper and not in the announce, and `MassWhisper` stopped double-punctuating a message that ends in its own "!". **Announces are summaries now**: `CoverageSummary` prints "Fortitude -- 18/25 Applied (72%)" plus the suppliers, naming the missing (behind an explicit "Missing:", since a bare list after a forwards-counting fraction could read as the ones who have it) only at `MAX_NAMED_MISSING` (5) or fewer -- a roll call of two dozen names is a wall nobody reads. Paladin lines get the same treatment. Tooltips group by modifier -- Alt block, blank, Shift block -- and the window's no longer lists the row shortcuts, which now sit on the rows themselves alongside Alt-Drag. Local `settings.buffingBar*`: master toggle + a Developer "preview as any real/fake paladin" dropdown; fake-raid pets show grouped but aren't castable (no real unit).
  In PP source mode a grayscale Engineering-icon button stays at the far right, keeps the `PP Buffs` bar at button height even when PP assigns nothing, counts active real-raider classes with no PallyPower class blessing assigned to that paladin, and opens PallyPower's Blessings window when the addon is installed. Pets, fake raiders, and Non-raiders do not contribute. The bar always reserves at least three horizontal button slots.
  **Self-buff buttons** — "Paladin Aura Helper" / "Righteous Fury Reminder" (`settings.buffingBarAuraButton` / `buffingBarRighteousFury`, both default on, Settings > Paladin Bar, rows 2 and 3) lead the row at the left, split from the class buttons by a 1px divider in a 9px gap; the divider only draws when a class row follows it, and both buttons act on the LOCAL player (test mode renders them for layout preview only). Both gate on a RESOLVED paladin, which is the whole class check — outside test mode `ResolveBarPaladin` only names one when the local player is a paladin, inside it only when one is picked — so test mode with an empty paladin dropdown falls through to the bar's "No Paladin selected for testing." hint instead of offering buttons for nobody. Neither shows a coverage count — the slot under the button stays empty except for a countdown. **Aura swapper:** `WhoDoesWhat.PaladinAuras` (Data.lua) lists the client's auras by BASE-rank spellId — Devotion, Retribution, Concentration, the three resistances, Sanctity, plus Crusader Aura on TBC — with names/icons resolved off the id at load and nameless entries dropped. The bar filters with `GetSpellInfo(name)`, which is what keeps Crusader Aura out before level 62 -- but that is the client spell database, NOT the spellbook: it resolves talent auras a paladin never specced into (and `IsSpellKnown` on the base-rank id proved no better), which is how Sanctity Aura turned up for a paladin without the Retribution talent granting it. The talent tree answers it instead, BY NAME: auras flagged `talent` in `PaladinAuras` (only Sanctity Aura -- Concentration is trained, its talent merely improves it) go through `TalentGrantsAura`, which asks `GetOwnTalentRankByName` for a talent carrying that spell's own name across all three trees. By name and not by (tier, column) because a talent granting a spell carries that spell's name, both strings come from this client so it is locale-proof, and unlike a coordinate it cannot be quietly wrong about where a tree puts things -- which is how the coordinate cut of this failed. No talent of that name at all means this client has none, which shows the aura rather than silently deleting one. `GetOwnTalentRankByName` calls `GetTalentInfo(tab, i)` plainly, with none of the isInspect/isPet/group arguments the inspect scans pass: a group index the client dislikes returns a nil name for every talent, which reads as "no such talent" and waves the aura straight through. Test mode also stopped short-circuiting the whole filter for a real paladin -- it returns the full client list only for a NON-paladin, who has no spellbook to ask; these buttons cast for YOU whoever the bar renders, and that blanket return was hiding every filter from the one person able to notice. The answer is remembered per aura key and retired on SPELLS_CHANGED (registered on the loader, not the bar's own frame, whose handler is gated on being shown): it only moves on a respec, and this sits on a path that can repaint at 10Hz. Hovering opens a picker of every castable aura, left-click casts the one being offered; the swapper carries **no tooltip of its own** — the picker replaces it. The picker (`CreateAuraMenu`/`ConfigureAuraMenu`) is a protected panel built like the class buttons' player menus — shown from the swapper's `_onenter` snippet with `RegisterAutoHide`, so it closes on mouse-out — carrying the player menus' own header strip verbatim (same `PLAYER_HEADER_H` geometry, colours and blue click hint (one line here, "Left-click = Swap + Cast", centred in a strip cut down to `AURA_HEADER_H` for it: every icon in this panel swaps and casts, so there is only one thing to say)) and floored at `PLAYER_MENU_W` so that hint has room, with its contents inset `AURA_PAD` from the panel edge -- the padding comes out of the slack under that floor, so it does not widen the panel unless a row genuinely outgrows it, with the icons laid out below in captioned rows: "Auras" first, then "Resistances" off the `resist` flag in `PaladinAuras`. Each icon shows the CLIENT's own spell tooltip on hover — `SetSpellByID`, falling back to a `spell:` hyperlink on builds without it — off the rank the paladin actually knows (`select(7, GetSpellInfo(name))`, which resolves the highest rank for a rank-less name; the base-rank id covers test mode), plus a green "Running." line. Each icon is a `SecureHandlerStateTemplate, SecureActionButtonTemplate` button (that ORDER and that pairing are load-bearing: `SecureHandlerClickTemplate` owns an OnClick of its own, so listing it after the action template replaces `SecureActionButton_OnClick` and the button silently stops casting -- the first cut of this did exactly that, selecting the aura without ever casting it; State carries the same handler machinery and hooks no OnClick) carrying its own `astep`/`auraMacro`, and its snippet writes both onto the swapper through a frame ref, so picking an aura keeps working in combat. **Picking also casts** — the same `/cast` string is the option's own `macrotext1`, so the click that chooses an aura is the click that puts it up (header hint: "Click an icon = swap + cast"). That makes the option a cast button, so it registers both click edges like every other one here, and closing the picker moved into the wrap.s POST body gated on `down == false`. Both halves matter: a PRE body hides the option along with its parent before the button's own click handler runs, and a hidden button casts nothing -- which is exactly why picking an aura at first only selected it -- while hiding on the PRESS would take the button away before a key-up client reached its cast. Compared against `false` rather than tested for truth deliberately — a build that stopped injecting `down` would leave the picker open until auto-hide instead of eating casts. The attribute writes stay unguarded, being idempotent across both edges, and `PostClick` (persist + repaint) takes the usual `if down == true then return end`; the layout and that baking are combat-locked like every other secure write on the bar. `WirePlayerMenus` is now `WirePopoutMenus` and hands the `otherMenus` close-list to the self-buff buttons as well as the class buttons — the swapper contributes its picker and closes the player menus, Righteous Fury owns no popout and only does the closing (`CLOSE_MENUS_SNIPPET`) — so sliding along the row swaps popouts instantly instead of waiting out the previous one's 0.25s auto-hide. Both self-buff buttons now take `SecureHandlerEnterLeaveTemplate`, so their tooltip/`OnLeave` scripts are **hooked, not set**, or the template's secure dispatch would be thrown away. The pick is persisted to `settings.buffingBarAura` from the option's own `PostClick`, and `UpdateAuraMenu` paints a gold border on the offered aura and full colour on the one actually running -- matched on the option's own `step` (its index into the castable list), never on its position in the panel: the resistance row splits the list, so the two part company from the fourth icon on, which had the border landing three early; with nothing saved the selection adopts whatever aura is already running, so a first login doesn't open on a false alarm. The icon greys with a red PixelGlow whenever the offered aura isn't the one running. **Righteous Fury:** appears only while the RENDERED paladin holds a tank role (`IsMarkedTank`, which is where prot talents reach this via role auto-detection) -- deliberately the rendered one, not the local player, so test mode previews that paladin's bar honestly. The cost is that a fake raid can hand the preview to a paladin who is not a tank and the button quietly goes, which reads as a bug; the title tooltip answers it with a red warning-icon "Previewing Bar for: X" line above the source line whenever test mode is on; one fixed self-cast, so its secure attributes are set once at creation. Red glow + grey icon when it's down, yellow glow with an `m:ss` countdown in its last ten minutes, nothing for the rest of its half hour. Both repaint from the bar's existing 0.5s tick plus a `UNIT_AURA`-on-`player` shortcut (which updates only these two buttons rather than running the whole repaint), so a cast lands immediately; `SetButtonGlow` now tracks the glow COLOUR so the red↔yellow switch restarts the animation. **Every tooltip the bar owns** — the two self-buff buttons and the title strip — opens where the class buttons' player menus do, so none of them lands on top of the bar: `PositionPlayerMenu`'s direction rule is factored out as `PopoutDirection(frame, needed)` and all callers use it, anchoring `TOPLEFT→BOTTOMLEFT` (down) or `BOTTOMLEFT→TOPLEFT` (up) off the `buffingMenuGrow` setting with the same flip when that side is cramped. Because GameTooltip only knows its height once populated, `ShowBarTooltip` owns it with `ANCHOR_NONE`, calls the frame's own `FillTooltip`, shows, then re-anchors; an optional `frame.tooltipAnchor` hangs it off something else, which is how the title strip anchors to the whole bar rather than to the strip (anchoring to the strip would put it over the button row). `RefreshBarTooltip` re-runs that whole build from every repaint path — guarded on `GameTooltip:GetOwner()` still being that frame — so a rotated aura, a landed cast and a ticking countdown all update under the cursor.
* **Warrior Shout Bar (WarriorShoutBarView.lua):** a small movable strip of one icon per warrior shout — Battle Shout always, Commanding Shout on TBC (`WhoDoesWhat.WarriorShouts` in Data.lua, base-rank ids resolved to name/icon at load and dropped if the client doesn't know them). **Secure, left-click to cast** (`SecureActionButtonTemplate`, `RegisterForClicks("AnyUp", "AnyDown")` — both edges, same as the paladin bar and PallyPower, because secure buttons obey `ActionButtonUseKeyDown` and an up-only registration simply never fires on a client set to act on key down; the price is that `PostClick` runs twice per click, which its `if down then return end` drops). `ConfigureShoutButton` writes `type1`/`spell1` (rank-less name → highest known rank) and no-ops unless the button's shout actually changed, which is rare — the paladin bar's per-refresh rotation rebake doesn't exist here. What the template does cost is layout discipline: `RefreshWarriorShoutBar` and `UpdateWarriorShoutBarVisibility` both bail to a counts-and-glow-only repaint under `InCombatLockdown()` (no creating, showing, hiding or moving a secure button or the frame parenting it), Alt-drag refuses to start in combat, and `PLAYER_REGEN_ENABLED` on the loader frame settles whatever was deferred. Each icon greys out when covered and turns full-colour with a LibCustomGlow PixelGlow while anyone who wants that shout is without it — **red on nobody, yellow once partially up** (`SetButtonGlow` takes a colour, not a bool, and tracks `glowColor` so a red→yellow switch actually restarts the glow instead of keeping the colour it started with); a covered/total count sits under it (`CountColor`: green only at full, yellow the moment it isn't, grey when nobody in the party wants it — the glow already carries the "none at all" alarm). **No title strip**: at one icon wide there was no room for a caption, and the strip was the only thing ever making the bar wider than its icons, so the bar is now sized to the icon row exactly. Alt-drag works anywhere on it including over the buttons, and shift-right-click on a button opens the settings page from `PostClick` (right-click casts nothing — no `type2` is ever set). The tooltip's missing list carries roster members rather than names so it can render them like the status-bar tooltips: role icon (hunter-pet role for a pet, class icon as the fallback for an unset role) + class-coloured `DisplayName`, local player first and their pet right behind (`LineIcon` / `ColoredName` / `IsLocalPlayerEntry` / `SelfFirst`, deliberate small copies of StatusBarsView's locals — promote them somewhere shared if a third view wants them). **Everything is scoped to the local player's PARTY** (`PartyNames`: the subgroup off `GetRaidRosterInfo`'s third return in a raid, self found via `UnitIsUnit` to dodge the realm-suffix question; nil = party/solo, nothing to narrow) — a shout reaches the caster's party and no further, so a raid-wide count would report gaps nobody here can close. That covers the coverage counts AND the warrior count driving how many icons show. Hunter pets are asked about by `owner` (`InParty`), since a pet is wherever its hunter is. Targets come from `HasBuff` — Battle Shout goes to whoever `WhoDoesWhat:WantsBattleShout` says wants it plus summoned hunter pets; Commanding Shout goes to everyone including pets (`everyone` on the shout). Fake raiders, non-raiders, disconnected members and **the dead** are excluded — the last one deliberately unlike `IsEligibleCoreBuffTarget`, which keeps corpses on its lists out of combat because they are about to be rezzed and rebuffed; a shout is cast on whoever is standing there when you press it, so a corpse is never a target. Optional `shoutBarIgnoreOutOfRange` (default off) additionally drops anyone `UnitInRange` says is far away — the standard ~40 yard test, wider than a shout's real radius on purpose, so it removes the raider in another wing rather than the one standing a step too far back; a pet is range-checked by its owner, and anything the API declines to answer stays counted. unscanned (`nil`) is unknown, not missing. Frame strata is MEDIUM like the paladin bar, with `SetFrameLevel(20)` to clear ordinary HUD frames sharing it — briefly HIGH while an invisible bar was being chased, but the culprit there was the default position (now dead screen centre) and HIGH is where the world map lives, so the bar drew over it. Strata beats level, so the level bump can't reintroduce that. **A party with a lone warrior gets one icon**, showing `shoutBarSoloShout` (`GetSoloShout`, defaults to Battle Shout) — plain right-click swaps it (`ToggleSoloShout`, out of combat only since it rewrites a secure attribute; `btn.isSoloIcon` gates it so right-click does nothing when both icons are up). A second warrior arriving restores both icons to their canonical slots and the remembered solo choice survives for when they leave: one warrior can only keep one shout up, and the second appears at two warriors. Aura names are registered in `BuildNameMap` (BuffTracking.lua) directly rather than as `StatusBarChecks`, since nothing else reads them; repaints ride `RefreshBoardViews`, while visibility rides `GROUP_ROSTER_UPDATE` on the module's own loader frame (the bar may not exist yet to hear it). **Expiry countdown**: `MissingFor` also returns the soonest remaining duration among the covered (`GetBuffTimeRemaining`) — whoever loses it first is when the shout next needs casting — stored on the button as an absolute `expiresAt` so the bar's 4Hz `OnUpdate` can count it down with arithmetic alone, no aura rescans. `UpdateShoutTimer` draws it over the icon (18px outlined, face borrowed off `GameFontNormal`) inside the last `shoutBarTimerSeconds` — 30 (default) / 15 / 10 / 0 for Off — and hides it otherwise. **`shoutBarHideWhenBuffed`** fades each icon to alpha 0 **individually** while ITS shout is on everybody, restoring it the moment somebody loses it or its countdown starts (`ButtonIsIdle` / `ApplyIdleFade`, re-decided on the same 4Hz tick) — a covered Battle Shout goes quiet while a Commanding Shout somebody is missing stays put. The backdrop belongs to the row, so the bar itself only fades once no icon is left lit (and must stay lit for any icon to show, since alpha multiplies down). Deliberately alpha and not `Hide()`: a hidden button stops being repainted (refresh and tick both walk shown buttons only), so it could never notice the lapse and would have no way back — and alpha sidesteps the lockdown for a flag that flips mid-fight constantly. The cost is that a faded icon keeps its slot rather than the row repacking around it, since moving a protected button is exactly what combat forbids. The mouse comes off with it so an invisible icon doesn't swallow clicks, but THAT part is lockdown-bound, so an icon that fades mid-fight keeps catching clicks in its footprint until `PLAYER_REGEN_ENABLED`. Settings > Warriors carries `shoutBarShow` (`warriorOnly` default / `withWarrior` / `always` / `never`; every mode but `always` also requires a warrior in the party), `shoutBarAnchor` (`LEFT`/`CENTER` default/`RIGHT` — which edge holds still as the second icon comes and goes, encoded as the saved anchor point exactly like the paladin bar's grow setting, with `SetShoutBarAnchor` doing the save-then-load re-derive so switching it doesn't move the bar), and `shoutBarHideBackground` / `shoutBarHideNumbers` (the backdrop keeps its inset when hidden so icons don't shift; hiding numbers shrinks the bar by `COUNT_H`). Alt-drag to move (`shoutBarPos`).
* **Melee Hunter role (`hunter_melee`):** a fourth Hunter role wearing the Beast Mastery icon and sharing the hunter blessing order. It carries no spec meaning — it says the hunter stands in melee, which is what puts them inside a Battle Shout. Every role entry carries `wantsBattleShout`, attached at the end of `PopulateRolesAndCategories` and derived from the Might bans (`PaladinBuffBansByRole`) unless `BattleShoutWantedByRole` overrides it — which it does only for the hunters. `BattleShoutWantedByClass` is the fallback for an unset/unresolved role and for custom roles.
* **Talent sync (TalentScanning.lua):** `TALENTS_READY` → `AutoAssignDetectedRole`: fill empty, follow respec, never fight manual override on unchanged spec (compare `db.profile.talentSpecs`). A client's FIRST detection (`talentSpecs` nil) never overrides an existing assignment — it may be manual or synced from a client that could inspect in range; without this, two clients with different caches tug a player between roles. **Single-writer Blizzard flags:** the Blizzard role flag (`UnitSetRole`) has no range check AND every write fires `GROUP_ROSTER_UPDATE` on every client — which drives each client's reconcile sweep. Two clients with differently-stale boards therefore ping-pong one player's flag forever at the sweep interval, re-announcing "X is now Damage Dealer" on each bounce. Two defences, both required:
  * **Election** (`BlizzardRoleWriter` in Core.lua, behind `CanSetOthersBlizzardRole`) names ONE writer for OTHER members' flags. The LEADER wins whenever `Sync:RunsCompatibleProtocol(leader)` — the PROTOCOL match matters, not mere presence: senders are recorded as peers *before* the protocol check, so an out-of-date leader reads as "runs the addon" while ignoring every board we send, and electing them means their stale board stomps everyone forever. Without such a leader, the alphabetically first permitted member on our protocol is elected — name order is identical on every client, so everyone picks the same writer with no negotiation round-trip, which is what keeps ten assistants from writing at once. nil (nobody eligible) means nobody writes, which beats everybody writing.
  * **Settle delay** (`SETTLE_SECONDS`/`pendingSettle` in UnitMenuExtensions.lua) fixes the flicker the latch can't see, because it isn't a loop — it's one stomp and one correction. Every client writes its OWN flag the instant the user picks a role, but the board justifying it rides the 2s sync poll (`POLL_INTERVAL`), while the flag write fires `GROUP_ROSTER_UPDATE` and wakes everyone's 1s roster sweep (`TalentScanning.lua`). For ~1s every other client can see the flag change but not the board change, and "corrects" from stale evidence; the board then lands and the flag flips back. So a **sweep** (`ReconcileBlizzardRoles`, which passes `deferrable`) that finds the flag on a DIFFERENT REAL role records it, writes nothing, and re-checks after 5s via a self-arming timer — if the board catches up the disagreement evaporates at zero writes. `NONE` is never deferred (kick/reinvite, nothing in flight). Explicit user actions never wait. Paired with `Sync:PushSoon()` (0.25s debounce, called from `SetAssignedRole` **before** the flag write), which removes the race at the source rather than mitigating it. Considered and rejected: parsing `ROLE_CHANGED_INFORM(_WITH_SOURCE)` off `CHAT_MSG_SYSTEM` — unverified on 2.5.x, and it reports who set the flag rather than what the board says, which is the thing that was stale.
  * **Write latch** (`lastWritten` in UnitMenuExtensions.lua) breaks the loop the election can't: your OWN flag is always yours, so the player themselves is always a potential second writer. Keyed by ROLE ID, so it self-invalidates when our board's opinion changes — no hunting write sites. If we already wrote this role id and the flag now reads a *different real* role, we stand down (logged under `logRolePromotion`) instead of re-asserting. A flag reading `NONE` is not contested (that's the kick/reinvite reset the roster sweep repairs), so it still re-applies freely. Both sides stand down after one exchange and the flag settles on whoever wrote last; the underlying board disagreement is a version problem to fix at the source.
  * `ReconcileBlizzardRoles` remains the sole other-member flag write path (own edits, `Sync.ApplyState`/`ROLE`, roster sweep, `PLAYER_REGEN_ENABLED`); `AutoAssignDetectedRole` no longer gates on authority — `ApplyBlizzardRole` decides internally whether to write, queue an issue, or do nothing. `UnitSetRole` everywhere only fires when the flag actually differs (re-setting an identical flag re-announces to the group and fights other role addons). Quiet (no announce). **Combat:** patch 2.5.6 protects `UnitSetRole` and `RaidFrame:Show` from addon execution, so role writes and the tank promote window are deferred until `PLAYER_REGEN_ENABLED`. The **main-tank demotion is deferred too, by choice rather than protection** — `ClearPartyAssignment` is callable mid-fight, but re-promoting isn't (`SetPartyAssignment` is protected: Raid tab → right-click → menu, during the pull). A fight that swaps someone tank→dps→tank would charge the leader that every time, so the mark is held for the duration and the same `PLAYER_REGEN_ENABLED` reconcile demotes afterward if they're still off a tank role. **The demotion half is independent of the flag half:** it runs for ANY role including `nil`, because "role cleared" and "role has no `wowRole`" both mean not-a-tank. `ReconcileBlizzardRoles` therefore no longer filters its loop to members with a resolvable `wowRole` (that filter was what left a main tank marked forever after a clear or a switch to a wowRole-less custom role), `SetAssignedRole` calls `SyncBlizzardRoleState(name, nil, unit)` on the clear branch, and a contested flag stands down without skipping the demotion. Marking a main tank **Non-raider** (`wowRole=false`) now demotes them too. Undetectable roles (warlock_firetank, druid_dreamstate, feral tank, customs) untouched. **Master opt-out:** `settings.manageBlizzardRoles` (Settings > General, `WhoDoesWhat:ManagesBlizzardRoles()`) gates `ApplyBlizzardRole`, `ReconcileBlizzardRoles` and `StartPromoteWatch` — WDW's own board is unaffected, only Blizzard's group state is left alone. Local and deliberately unsynced: it's a per-client escape hatch for a raid where flags are being fought over, and one raider opting out must not opt everyone out. Re-enabling runs a reconcile immediately rather than waiting for the next event. Flags: `LOG_TALENTS`/`LOG_TALENT_COMMS`.
* **Paladin buff-talent scan:** per-paladin ranks — Improved Might (0-5), Improved Wisdom (0-2), Kings (0-1), Sanctuary (0-1); Kings/Sanctuary are talent-GRANTED (untalented can't cast). Stored in `db.profile.paladinBuffTalents` (nil = unscanned, ≠ rank 0). Ranks read from the client's **native `GetTalentInfo`** matched by hard-coded `(tab, tier, column)` — NOT the library's positional cache, whose index order is shuffled on the Anniversary client (LibClassicInspector assumes native order == its static table; it isn't, so its ranks land under the wrong talent). Only reads on a fresh inspect (`isInspect`) or the local player — a cache replay/broadcast has no live native data, so it's skipped rather than zeroed. `active` spec only (via `GetActiveTalentGroup(isInspect)`), which is what they can cast now. Versioned wipe: `buffTalentScanVersion`.
* **Kick/reinvite catch:** `TALENTS_READY` fires only on data arrival, so rejoiners sit stale; debounced `GROUP_ROSTER_UPDATE` sweep replays from cache (cache-time gated) + `ReconcileBlizzardRoles` re-applies drifted role flags (leader-only for others; see the single-writer note above).
* **Local-player self-scan:** the library never fires `TALENTS_READY` for the player, so a `selfSync` frame replays the pipeline for our own GUID on `PLAYER_ENTERING_WORLD` / `CHARACTER_POINTS_CHANGED` / `PLAYER_TALENT_UPDATE` / `ACTIVE_TALENT_GROUP_CHANGED` (dual-spec swap changes castable blessings). Last two `pcall`-guarded (not on every build).
* **Assignment model (Assignments.lua):** on `WhoDoesWhat.Assign`. Section defs are MODEL-ONLY vocabulary (key/title/store/noun/whisperLead/spells/targetPlayer/autoRows/multiMarker/GetWarning — view-only flags were removed when the sections were hard-coded into Views/Sections/), `SectionByKey`, `GetEligibleMembers` (Developer Mode lifts filter) / `MembersOfClass` (strict), marker/spell/target text (`{skull}` tokens vs words), whisper collectors (named per section: `CollectTankWhispers` etc., generic ones kept for future sections) + `MassWhisper` (0.25s stagger vs throttle), `ComputeBuffGrid`/`ComputePaladinBuffSummary`/`CompileBuffRules` (derived paladin coverage + custom rules — see below), `AutoAssignWarlockCurses`/`ClearTankAssignments`/`ClearMisdirectAssignments` (exported for the views' header buttons), `PruneDepartedAssignments`, `SetAssignment` (exclusivity + pairWith + repaint; curses only now).
* **Main window (/wdw):** two scrollable columns. Both dimensions track the view mode in `ApplyViewMode`: full board = `FRAME_W` 892 wide; Paladin-only narrows to `NARROW_FRAME_W` 380, hides the permission strip so it can't collide with the buttons, drops the scrollbar (`scrollBarHideable` + reclaims its reserved width by anchoring the scroll to `-MARGIN`) and centers the lone box (`f.columns[COL_LEFT].x` offset). Height auto-fits the visible content each refresh (`f.scrollTop + content height`, clamped to `MIN_FRAME_H`..`MAX_FRAME_H` 130..550) so Paladin-only view is a short low-profile window and the full board grows until it scrolls. Boxes anchor-chained within their column (height change ripples only that column). Uneven: `LEFT_COLUMN_W` 330 (the compact Buffing Rules minimum for Paladin Buffs/Warlocks), right 500 (Tank + busy dynamic rows — CC/Misdirect + future custom sections). Section headers sit 5px below the box top with no separator before the first row; their titles use the normal UI font at +2 size, near-white, with a thin outline. Row dividers are replaced by touching, fully opaque alternating fills blended toward white. Paladin, Warlock, and Misdirect panels derive strong dark tints from the Paladin, Warlock, and Hunter colors in `Data.lua`; Tank and CC remain on the neutral section palette. Scroll child needs explicit `SetPoint`. `RefreshMainAssignmentsView()` repaints externally. Sections are hard-coded (see Views/Sections/ in Files); the bullets below describe each section's behavior. **Paladin-only view:** local `settings.paladinOnlyView`, controlled only by the main-window "Full view" checkbox, hides every section box but Paladin Buffs regardless of assignment state. `RefreshAll` ends with `ApplyViewMode` (box `SetShown` → `K.LayoutColumnBoxes` re-anchors visible boxes per column, skipping hidden → `K.UpdateContentHeight` ignores hidden boxes) then `UpdateViewToggle`.
  * **Dynamic row sections** (Tank, CC, and Misdirect in the right/wide column): all assignment rows use the Paladin summary's compact 24px height and 20px role-icon scale. CC uses pooled rows `[player] [spell] [marker] [edit] (!) [x] [mail]`, and Tank similarly omits a separator arrow between its fixed player label and marker picker. CC rows are added via the header's "Add (+)" (title strip, not bottom), while tank + misdirect rows are auto-managed (`autoRows`, below). Header buttons are row-button-sized (22px, -2 gaps) so header mail/X align with the rows' mail/x columns; the title strip is 22px and rows start at -30 without a separator line. In board view-only mode, Tank/CC/Misdirect compact their pooled display onto only complete rows (player plus every required spell/target/marker), remapping each display slot to its original saved entry; edit mode retains incomplete rows so editors can finish them. CC entry: `{player, marker=1..8|"all"|"custom", custom, spell}`; tank entry: `{player, markers={mix of 1..8|"all"|"custom"}, custom}` (array, kept skull-first by `NormalizeMarkers`).
    * Tank (`tankAssignments`, `autoRows`+`multiMarker`): **one row per marked tank, auto-managed** like misdirects — fixed player label and no Add. `EnsureAutoRows` runs during talent scans, direct role changes, and debounced roster lifecycle reconciliation; section refreshes only render, so opening the window cannot mutate or sync the board. The marker dropdown is a **multi-select** (incl. "Everything else"/Custom) so one tank holds all their markers on one row; selecting a new option closes the menu, while deselecting a checked option keeps it open. Toggles route through `SetTankMarkerPlayer`/`RemoveTankMarker` (one tank per marker — checking steals the value off its holder; misdirect sync + adoption ride along). Each row's **[x]** clears that tank's markers/custom text without removing the auto row. Read-only rows retain the edit row's normal-sized role icon and player label, followed by marker text instead of a full-line small-font stand-in. `KeepStray` keeps rows for marker-holding players who aren't marked tanks (unit-menu assignments) so they warn instead of vanishing. Warns unmarked player / empty markers. Header **[x]** (confirm popup `WHODOESWHAT_CLEAR_SECTION`) wipes all tank assignments; auto rows immediately repopulate with empty/default dropdowns and inherited misdirect markers clear.
    * CC (`ccAssignments`): spell dropdown from `CCSpells` filtered to assignee's class (full list while unassigned); incompatible reassign clears spell. Read-only rows mirror Tank's normal-sized role icon/player name, followed by spell details and a 20px target-marker icon.
    * Misdirect (`mdAssignments`, `targetPlayer`, `autoRows="Hunter"`): **one row per hunter, auto-managed** — no Add/per-row [x]; `EnsureAutoRows` reconciles the store to the roster (retained rows keep their order + tank/marker, newcomers append, departed drop) in the model's debounced roster lifecycle handler, gated on edit permission (read-only clients render the leader's synced rows). Shared by the tank section via `autoRoster` (marked tanks) + `KeepStray`. Header **[x]** clears all assignments and immediately repopulates one blank row per hunter. The hunter column is a fixed label, not a picker; you only fill in each hunter's tank. Tank + marker: marker inherits the tank's first tank-marker — defaulted on tank pick (`FirstTankMarker`) and re-synced whenever that tank's markers are (re)assigned (`SyncMisdirectsForTank`, called from every tank-row change + the unit-menu tank setters), still hand-overridable via the row dropdown. Reverse too: assigning a tank to a marker adopts any tank-less misdirect already sitting on that marker (`AdoptMisdirectsForMarker`, empty targets only — never steals). Warns non-hunter/multi-misdirect/unmarked tank/tank-with-no-marker/marker-the-tank-isn't-tanking.
    * CC header: "X" clear-all behind `StaticPopup` (`WHODOESWHAT_CLEAR_SECTION`, `preferredIndex=3`); disabled when empty.
  * **Static sections (left/narrow column, `Sections`):** "Paladin Buffs" (computed summary, see next) and "Warlocks" (three tooltip-only Improved Healthstone rank icons in the header followed by the flavor's raid curses, mutually exclusive via `exclusiveWith`; TBC Elements floats for Affliction/Malediction, while Classic adds Shadow). Healthstone ranks are scanned natively at Demonology row 1 / column 1, remain unknown until inspected or received from that warlock, and gray out when no confirmed owner has that rank. The whole section disables with no warlocks; in view-only mode with no curse assignees, its fixed rows collapse to `No Warlock assignments yet.` Paladin blessings, fixed-row role icons, Warlock curse icons, and the inset Healthstone header art share the 20px icon size; player-dropdown role icons use the CC spell icon's 14px menu scale. Paladin summaries and Warlock curses share the compact 24px row height, and shortened curse labels use the same normal assignment font as Tank names. Dropdowns float preferred above divider. Every assignment dropdown (options + collapsed selected value) prefixes the player with their role's spec icon (`RoleIconMarkup`/`PlayerTextWithRole`; "" when roleless), including the preferred-paladin buff-rule picker.
  * **Paladin Buffs section (`paladinSummary`):** NO assignment — blessings are never stored per paladin; coverage is derived (`ComputeBuffGrid`). One pooled read-only 24px row per paladin: `[role icon] Name  [buff] (n) > [buff] (n)`, up to 3 buffs each (count-desc, canonical ties), paladins sorted by total workload desc (`ComputePaladinBuffSummary`). Each row can whisper its Paladin; there is no header mass-mail control, and Buff Grid lives in the main toolbar. The right-aligned header labels the raid-wide `PallyBuffSource` as `Mode:`; its permission-gated dropdown expands to `WDW Assignments` / `PallyPower`, collapses to `WDW` / `PallyPower`, explains source-of-truth behavior on hover, and rides the synchronized board snapshot. A background-free PallyPower sync/action row extends the header above the Paladins: WDW mode shows `[!] X Buffs in PP don't match the plan [Diffs] [Fix]`, while PallyPower mode shows yellow `[!] X unoptimized buffs [Examine]`. The PP-mode count ignores provider swaps when the same blessing remains covered, counting only missing blessing types and confirmed talent downranks; the Differences window and `Fix All (X)` retain the strict number of cells WDW would change. Examine and Diffs open that same comparison window. It leaves a 6px gap before the striped list. A 4px gap separates the summary/empty rows from the second "Buffing Rules" header; the entire rules area hides and the box contracts in PallyPower mode. In WDW mode its "Add (+)" and clear-all X hide in board view-only mode. Section grays with no paladins (`disableWhenNoClass`); box resizes + ripples its column.
* **Buff rules (`paladinBuffRules`, "Buffing Rules" > "Add (+)"):** WDW-mode editable 24px rows under the summary — `[buff v] [Is Ignored / Prioritized for / Preferred by v] [target v] [x]`, with the first two pickers condensed to 68/80px, one rule per blessing, max 6 (buff dropdown offers only unruled blessings; new rules start as inert "Preferred by (choose)"). Ignore = buff drops from the plan entirely; Prioritize (Everyone / Tanks-Healers-DPS / class → level-2 "All class"+roles) = buff jumps to front of matching raiders' orders AND their demand votes (`RuleAdjustedOrder`); Prefer by paladin = hard primary lock (+100 in the matcher, beats any rank gap — position still dominates so unwanted buffs aren't force-fed; lock also removes paladin+buff from the primary demand race). Prefer's paladin dropdown floats capable casters (known talent rank > 0) above a divider and annotates everyone — (talented)/(can't cast) for Kings/Sanctuary, (n/max) for Might/Wisdom, (not scanned) — and a row (!) warns when the named paladin has a known 0 in the buff's talent (gated: rule inert; Improved: blessing unimproved). Shared as `STATE.paladinStrategy`, permission-gated like the assignment board, cleared on group leave, and automatically pruned by the leader when a preferred paladin departs.
  * **Auto-assign:** Warlock only — TBC puts Elements on Affliction (else first) and Recklessness on a second warlock; Classic fills Recklessness, Elements, then Shadow on distinct warlocks. Settings gate the magic curses and Recklessness separately.
  * **Mail:** per-row whisper of full job list (disabled while unassigned); header mass-mail whispers all assigned (self filtered). Whispers use marker tokens; `:Print()` uses words (`AddMessage` won't expand tokens).
* **Buffing Grid (BuffingGridView.lua):** the former Raid Buff Grid and Paladin Full Grid are one no-scroll window. Each block starts with the status columns enabled on the **Buff Tracking** settings page, followed after a visual gap by one 26px planned-blessing column per paladin; provider-class columns hide when that class has no active raider. Dead is hard-disabled as a grid column. The same per-check mana, tank, and Hunter-pet filters drive both coverage math and grid cells. Negative checks show nothing while absent and overlay the ready-check X on their aura icon while present; their tooltips report the debuff and last observed remaining time. Header tooltips come from `StatusBarChecks` metadata; Mark/Fortitude/Thorns also list provider talent ranks. The window sizes to content and splits into two blocks at ≥20 raiders. Planned blessing cells still use `ComputeBuffGrid`; confirmed missing blessings are desaturated behind a red border. Paladin coverage remains derived; nothing is stored in the view.
  * `Show Pally Buff Source` is one WDW / PP Mirror / PP Addon dropdown. Each window open resets it from the synchronized raid source: WDW mode selects WDW; PallyPower mode selects the live PP Addon tables when available, otherwise the observed PP Mirror. WDW mode warns on either PP comparison view; PallyPower mode accepts both PP views without warning. A mismatched view warns that its paladin cells are comparison-only rather than the raid's active assignment plan; live missing-buff indicators remain live.
* **PallyPower bridge (PallyPowerBridge.lua):** `SyncToPallyPower` maps the computed grid onto PallyPower's model (one Greater Blessing per class per paladin; minority raiders become Normal-blessing exceptions), updates a co-installed PallyPower's local tables when present, and broadcasts its wire protocol (CLEAR SKIP → PASSIGN per paladin → batched NASSIGN). Without PallyPower installed, WDW sends the protocol directly and uses its observed-board mirror for Diff/Fix status and minimal role-change pushes. Receivers only accept assignments for others from a leader/assist sender (or Free Assignment) — button warns without that authority. Fake-raid names never hit the wire. **No auto full-resync or diff-window popup** — a paladin joining/leaving or turning Non-raider reshapes the whole grid but touches nothing in PallyPower, so the passive assignment/status UI surfaces real coverage drift through `CheckPallyPowerSync`; users open the formatted **PallyPower Differences** window manually when they want the details. The check compares each active target's effective blessing after Normal overrides, not the raw Greater/Normal table shape, because multiple layouts can provide identical coverage. `PushPlayerBuffToPallyPower(name, priorDiffs)` is the minimal role-swap path: from an aligned baseline, if only that raider's effective blessings change, it sends only their NASSIGN rows (including `0` clears). Pre-existing effective drift or changes to other raiders leave PallyPower untouched for the passive UI to report. PallyPower-authorized leaders/assists send directly; ordinary-raider role changes are relayed once the WDW leader receives their synced role. Minimal NASSIGN changes parse in combat; PallyPower defers protected layout work until combat ends. While session logging is enabled, PLPWR traffic lands in `WhoDoesWhat.PallyPowerLog`, translated for `/wdw pplog`; logging resets off on reload.
* **Curse calculator (CurseCalculatorView.lua):** pulls a fight from Details!, prefers its recorded boss target with the prior max-damage fallback, and estimates raid damage provided (or foregone) by the flavor's curses. Classic splits CoE (Fire/Frost, optional 1.21x Ignite double-dip) from CoS (Shadow/Arcane), uses level-60 armor math/ranks/presets, and omits Malediction/Blood Frenzy; TBC retains combined-school CoE, Malediction, level-70 armor values, and Blood Frenzy. CoR reconstructs armor from user-ticked debuff assumptions, excludes known bleeds via spell-id list, and accepts optional average flat armor penetration. All estimates assume 100% uptime and deliberately do not value resistance reduction.
* **Members (MembersView.lua):** a fixed two-line overview strip sits between the title bar and the scroll area (`OVERVIEW_H`, doesn't scroll): line one is each bucket's icon + bare count, including zeros for the three ROLE buckets (the grids hide an empty bucket, so this is where "no healers at all" stays readable) but omitting the roleless bucket entirely at zero — it wears the warning icon, and a warning over a `0` announces the good outcome as an alarm, line two a gray `N members - N with WhoDoesWhat - N offline - N issues` (offline clause only when nonzero; the issues clause is orange when nonzero and a green **Nothing to fix** at zero — the only surviving piece of the old Action Items all-clear panel, because "no warning icons anywhere" is not the same as being *told* the board is clean before a pull). Below it, members shown in four borderless grids headed "[icon] N Tanks / Healers / DPS" and "[warning icon] N Unknown or Inactive" (a bucket with nobody in it is hidden, and the chain re-anchors past it); window title is a centred (not the shared left-aligned chrome) "Raid Members" in a raid, "Group Members" otherwise, with no "WhoDoesWhat -" prefix, with alternating class-tinted rows from `Data.lua`, class-then-name sorting, and six columns under per-grid headings on a second header line (`GRID_HEADER_H` 44, the bucket title keeping the first line to itself): a leading warning gutter, **Player**, **Has WDW?**, **Group role**, **WhoDoesWhat**, and **Talents** with a Rescan button pinned to the row's right edge. Dropdown mirrors the unit menu's Set Role exactly: class + custom roles, then a divider + **Non-raider** (`SetAssignedRole` with `NON_RAIDER_ROLE_ID`; shows the zzz icon, no warning, lands in the Unknown or Inactive bucket since `wowRole=false`), and a passive disabled "None" shown only while roleless — neither panel un-assigns a role back to roleless. Row icon = the assigned role's spec icon with the class icon as fallback; it is no longer given up to hold a (!), because the warning has its own leading gutter (`ISSUE_COL_W`) that LEADS the row rather than trailing it — trailing, the icon read as belonging to the column it followed rather than to the member it flagged. **One warning icon per member, not per column:** its tooltip is titled with the count ("3 issues") and lists every issue as a wrapped line separated by blanks, from `data.issues` (see Roster issues below). Disconnected raiders use the shared alternating gray row colors, a gray name and a desaturated role icon/dropdown, and their "Has WDW?" cell is left EMPTY (offline says nothing about the addon, and the row already reads as offline four other ways); `UNIT_CONNECTION` refreshes this live. The window auto-fits its height between `MIN_FRAME_H` and `MAX_FRAME_H` on every repaint (`UpdateContentHeight`); the scrollbar gutter is always reserved, but the bar only appears once the content hits the ceiling (`scrollBarHideable` plus an explicit `SetShown`, since the template only reacts to range *changes*) and wears the main view's AceGUI slider-track backdrop, hooked to follow the bar in and out of view.
* **Settings (AddonSettingsView.lua):** Option descriptions live in hover tooltips. General → Show Minimap Button (`settings.minimapButton`, default shown; the launcher is registered through the bundled LibDataBroker/LibDBIcon and adds nothing of its own on top — showing, hiding and fading are the user's minimap addon's job, which is what registering buys; an addon-owned `TempPortraitAlphaMask` overlay clips the opaque square source icon to the circular button; left/right-click open assignments/Buffing Grid; Shift-left/right-click open Members/Settings; LibDBIcon persists its drag position) + Announce Role Changes (`settings.announceRoleChanges`, default on — gates the "[WhoDoesWhat] X was changed to..." group message in `SetAssignedRole`; Log Operations and Blizzard's own role-flag message are unaffected). Developer Mode (lifts class filters, keeps gated sections live, opts out of CC auto-clear; warnings still fire) + Show Logs button (reveals the main-toolbar traffic-log button) + Log UI Updates (persisted → `LOG_UI_BUILDING`) + Log Operations (persisted → `LOG_OPERATIONS`; gates routine assignment, reset, auto-assign, role, customization, whisper, PallyPower-success, and manual-sync-success messages while leaving warnings and no-op explanations visible) + Populate Fake Raid.
* **Populate Fake Raid (FakeRaid.lua):** testing toggle (`settings.populateFakeRaid`) that appends `FakeRaid.ROSTER` (always 23 fakes: 3 tanks, 5 healers, DPS fill) at the single roster choke point `GetEligibleMembers`. `Rebuild()` assembles the roster deterministically (no randomness — identical on every client) from CORE + the first `settings.fakeRaidPaladinCount` (1–4, settings dropdown) of PALADINS (prot Kings+Sanct → ret Might5 → holy Wisdom2 → ret Might5+Wisdom2, the overlap exercising auto-assign tie-breaking) + a substitute resto shaman while Holy is out (count<3) + DPS_POOL in fill order. Count changes while live wipe the board and clear every possible fake name (`EachPossibleFake`, superset — the current roster may no longer include an old name) before re-injecting. Fakes' roles → `db.profile.assignments`, paladins' ranks → `db.profile.paladinBuffTalents`, and warlocks' Healthstone ranks → `db.profile.warlockHealthstoneTalents`, keyed by name, so the views treat them like real scans; local player is the 24th seat. `SetFakeRaidEnabled` wipes the board (raid/tank/CC/MD) on **both** transitions and adds/removes every fake trace. `ReapplyFakeRaid` re-writes the fakes' roles/talents while the toggle is on — called from OnInitialize and after Sync's group-leave board wipe (which can fire around reload/logout while solo and would otherwise eat them; re-inject sits before the wipe path's `Fingerprint()` refresh so it never reads as a local edit). Solo-testing tool — not guarded against broadcasting into a real group's sync.
* **All Roles ("Role Preferences"):** two-column class list, expand specs/categories, gear marker on customized, "+ Create Custom Role".
* **No automatic Blizzard-flag writes.** `ReconcileBlizzardRoles` used to run on a synced board, a member's `ROLE` broadcast, every roster event and the end of combat. All four are gone: the flag change and the board change reach a client by different routes at different speeds, so whichever swept first swept with stale evidence and overwrote a player who had just set their own role — the settle delay only postponed that, it didn't make a stale board right. The function survives for the one manual caller (re-enabling the Settings toggle); the settle-delay helpers went with the sweeps. The **only** writes left are a player setting their own role, someone with rights setting another's from a menu, and the Members window's **Group role** dropdown — all three MANUAL (see the manual/automatic gate split below), so a raid assistant can make them without being the elected writer. Consequences by design: a kick clears the flag and nothing restores it (it becomes an unset-flag issue on their Members row), and a write blocked by combat is never retried.
* **Roster issues (ActionItems.lua):** the model behind the Members window's warning gutter, the toolbar count and the WDW Status row. It used to back a separate **Action Items** window — which was the Members window filtered to the rows with a problem, wearing the same class tinting, the same roster line and the same role dropdown, and said so in its own header — so the two merged into `MembersView.lua`; what stayed here is the *judging*. `GetRosterIssues()` walks `GetGroupMembers(nil)` once and returns `byName, total, actionable`, where every member gets an entry (`unit`, `roleId`, `role`, `blizzRole`, `snapshot`, `talentRoles`, `mayRole`, `mayFlag`, `issues`) whether or not anything is wrong, so the view resolves none of it a second time and the icons can never disagree with the count. `CountActionItems()` is a thin wrapper for the two counts. Units come from one `UnitsByName()` roster walk keyed the same way `db.profile.assignments` is — resolving them a name at a time was quadratic on a 40-man list. **Five issue sources:** *pending* (no WDW role at all — the bulk of the list when a raid first forms and the one you actually work down), *stale role* (a saved id that no longer resolves, e.g. a deleted custom role — it needs a new pick, not a shrug), *disagreement* (see below), *unset flag* (on the board, but Blizzard has no flag for them at all — a kick clears it and nothing restores it; compared against nothing by the majority rule, since an absent flag is not a wrong answer, so it is stated outright), and *unpromoted tank* (a raid tank nobody promoted, advisory only since `SetPartyAssignment` is protected). The last one absorbed the old window's read-only *Main tanks* section: it is now a line in that tank's own tooltip, splitting on `IsRaidAssistant` between "Promote them in the raid UI" + the social/raid panel keybinding (yellow, in brackets, the game's own style; `TOGGLERAIDTAB` then `TOGGLESOCIAL`, since the raid tab has its own binding name only on some builds; omitted when unbound) and "Ask the raid leader or an assistant to promote them." The old section's tooltip roster of every leader/assistant from `GetRaidRosterInfo` went with it — identical text on every tank's row was not worth the roster walk. **Flag-derived issues are skipped solo and for fake raiders:** ungrouped, Blizzard reports your own flag as `NONE`, which would be a permanent complaint about a flag that means nothing until you're grouped, and a fake raider has no unit to read a flag from or promote. Role-presence issues still apply solo, matching the (!) the Members window has always shown there. `CountActionItems` returns **total, actionable**, where actionable counts issues this client could act on — `CanEditRoleOf` for board issues, `CanEditGroupRoleOf` for flag issues, `CanPromoteMainTank` for promotions; the Members button's gold label + `LibCustomGlow` and the status row's glow follow `actionable`, while the count itself stays the honest total. An unpermitted raider therefore still sees "Members (23)" in plain white and a status row reporting 23, and nothing pulses at them about roles that are the leader's to set. It also owns a **WDW Status row** (`actionItems` in `StatusBarCheckOrder`, first by default, `defaultEnabled = true`, wearing the client's micro role TANK atlas via `wowRoleIcon = "tank"` — the same shield the group-role dropdowns draw. `WhoDoesWhat:ApplyStatusCheckIcon(texture, definition)` is the texture-side twin of `GetWowRoleIconMarkup`: atlas when the check names a `wowRoleIcon`, else the `icon` file with the usual inset TexCoord. The reset in that fallback branch matters — the options panel's header texture switches between checks, and `SetAtlas` leaves its own coords behind.). The key stays `actionItems` after the merge: saved options and `statusBarOrder` are stored under it, and renaming would silently reset both. PallyPower and this are both "one line of state plus a click that opens the fixing window" rather than coverage bars, so StatusBarsView folds them into one `CreateStateRow`/`LayoutStateRow` pair driven by three buckets — `inactive` (grey, no icon: PallyPower absent / you're ungrouped), `ok` (green + check: synced / nothing to fix), `attention` (orange + warning, clickable). Only the icon, tooltip wording and target window differ, via `STATE_ROW_TYPES`; both rows open their window on a plain click **only while in `attention`** — there is nothing to look at otherwise, and the toolbar's Members button remains the surface that opens a clean roster on purpose. Its custom options mirror PallyPower's trio one-for-one: **Hide when nothing to fix** (`hideWhenClear`, default off), **Hide when not in a group** (`hideWhenSolo`, default on), **Hide when nothing is yours to fix** (`hideWhenNotYours`, default on — suppresses the row when every outstanding item belongs to someone else; deliberately gated on the `attention` bucket only, since hiding a *clear* list is `hideWhenClear`'s job, and on `actionable` rather than raw permission, so a raider with their own role still pending keeps the row), **Action items glow** (`actionItemsGlow`, default on, gated on `CanEditAssignments` the way PallyPower's is gated on `IsRaidAssistant` — a raider who can't change anyone's role doesn't want a pulsing row). `ActionItemsRowState` is skipped entirely when the row is off, because it walks the roster and status repaints ride the buff-tracking notify. A `Core.lua` migration inserts `actionItems` at index 1 of an existing saved `statusBarOrder` — `GetStatusBarCheckOrder` appends unknown keys to the END, which would bury it. **Repaint routing changed with the merge:** `RefreshActionItemsView` used to ride `RefreshBoardViews`, so "the board changed" quietly repainted the issues too. `RefreshMembersView` cannot join it — that is the 10Hz buff-tracking path the exclusion exists for — so the issues now reach the screen through the direct calls the roster paths already make (Sync, Permissions, `SetAssignedRole`) plus `RequestFullRefresh` for talent arrivals; `RescanPlayerTalents` and `RescanBuffingTalents` call `RefreshMembersView` outright, since the Rescan button lives on that window and has to grey the instant it is pressed.

**The merged row states the same fact three times over,** because the interesting part is where the three disagree. The **Group role** column writes Blizzard's flag directly via `UnitSetRole` (local `SetGroupRole`, gated on `UnitIsUnit(player)` or `CanSetOthersBlizzardRole`, and it clears the write latch so a later auto-write doesn't read the hand-picked flag as contested); the **WhoDoesWhat** column runs `SetAssignedRole`, which stores the spec and pushes the flag to match. Picking IS the fix, in whichever direction is right for that row. `SetGroupRole` is deliberately NOT gated on `ManagesBlizzardRoles()` — that switch stops WDW writing flags on its own initiative, and a person opening a dropdown IS the initiative. The WDW dropdown is the Members one, not the old Action Items one: it keeps the unit menu's full menu (class + custom roles, divider, **Non-raider**, and a passive disabled "None" while roleless). The **Talents** column is evidence, not a control: the role(s) the cached `LibClassicInspector` spread reads as, stated as icon + name exactly like the two columns beside it, with the raw per-tab spread one hover away (`GetTalentSnapshot`; a grey "not scanned" when nothing is cached — an unknown spread must not read as a player with no points) and a **Rescan** button pinned to the row's right edge. Feral druids get *two* roles (cat and bear share the tree, so the points genuinely can't tell them apart and one would be picking a side). Rescan runs `RescanPlayerTalents` — the single-player form of `RescanUtilityTalents`: replay the cache, then `DoInspect`. It is ungated by permissions, because an inspect writes nothing to anyone's board. While the request is outstanding the button reads **Queued** and is disabled; `rescanPending` maps player key → expiry, cleared by `OnTalentsReady` and expiring after `RESCAN_TIMEOUT` (10s) so an inspect that never answers — out of range, combat, logged out — doesn't leave a dead button for the rest of the raid. It is also disabled with no unit at all (a fake raider, or someone who has since left). **Disagreement is decided by plain majority, and the blame goes on the odd one out** (`RowDisagreements`, ActionItems.lua): a resto shaman the board and his talents both call Restoration, flagged `DAMAGER`, has *one* thing wrong with him, and naming all three would make the reader do the elimination the model already did. Each answer counts how many others it agrees with; anything below the best score, and in a disagreement, earns a sentence. When nothing scores an agreement — a two-way mismatch with no third answer, or a three-way split — there is no odd one out, so it emits ONE line naming the parties rather than accusing each in turn. Emitting one per accused answer was right while each lit its own gutter icon, but collapsed into a single tooltip it printed the same sentence forwards and backwards, and inflated the issue COUNT so that one wrong role read as two. Only answers we *have* take part: an uninspected player or an unset flag is unknown rather than wrong, and a warning on top of that would fire on most of the roster the moment a raid forms. Pairs that can't be compared sit out too — the group flag only names tank/healer/dps so it is compared at that coarseness, and `TalentsCanJudgeRole` excludes roles no spread can name (`warlock_firetank`, `druid_dreamstate`, custom roles), because blaming a Fire Tank warlock's TANK flag for his destro points is a warning no rescan could ever clear. The talent half exists at all because `AutoAssignDetectedRole` deliberately lets a *first* sighting lose to an existing assignment — a role picked before anyone could inspect them would otherwise sit on the board forever. **Collapsing three per-column warning icons into one gutter cost the at-a-glance read** — the icon's column used to name the guilty answer before you hovered — so the sentences now have to say it, which is why they are phrased as full clauses ("Group role (Damage) disagrees with …") rather than as labels. **There is deliberately no fix button** — neither per row nor a Fix All, and `GuessRoleId`/`PREFERRED_GUESS`/`suggestedId` went with them. Both applied a spec guessed from Blizzard's flag, which is the *least* trustworthy thing on the row: Blizzard drops every new raider on `DAMAGER`, and pending rows are most of the list, so "fix" mostly meant writing that guess onto the board in bulk. Talents are the real evidence, so the column offers them and you pick from the dropdown. **Permission and combat both simply disable the dropdown**, with the reason in its tooltip (`blockedReason`). The old window swapped an unwritable column for plain text on the grounds that permission is a standing fact while combat passes; the merged row disables in both cases instead, since a disabled dropdown already reads its value out, which is all a read-only column wanted to do — and one behaviour per row beats two. Rescan sits outside the whole scheme (looking at someone's talents needs no rights). The two gates differ by column: the WDW column takes `CanEditRoleOf`, the Group role column takes `CanEditGroupRoleOf(name, unit)`. That takes the resolved `raidN` token as a CONVENIENCE, not a requirement, falling back to the player key, since WoW accepts a group member name as a unit and `ApplyBlizzardRole` already relies on that (`unit or playerName`); requiring the token instead greyed the whole column and dropped the unit `SetAssignedRole` needs to push the flag. It returns `ok, reason`, and the reason is what the greyed dropdown tooltip shows, so rank and combat can be told apart on hover. **Manual and automatic flag writes are gated differently, and that split is the point.** A MANUAL write — a person picking a group role, or picking a WDW role and expecting the flag to follow — takes only `CanSetOthersBlizzardRoleManually()`, i.e. `PlayerCanSetGroupRoles` (the WoW rank the server itself demands: raid assist / party lead), plus own-row always. An AUTOMATIC write — talent detection settling a newcomer, the reconcile sweep pushing the whole roster — still takes the single-writer election `CanSetOthersBlizzardRole()`. `ApplyBlizzardRole(unit, playerName, role, manual)` carries the distinction, `SyncBlizzardRoleState` and `SetAssignedRole` thread it, and the four human-click call sites (unit menu ×2, Members dropdown ×2, PallyPower Diff, `RemoveRaidCustomRole`) pass `manual = true`. Manual writes also skip the `lastWritten` loop-breaker: standing down is for automatic re-assertion races, and a person re-picking a role someone else overrode is asserting on purpose. **Why the split:** the election returns the raid leader whenever the leader runs WhoDoesWhat, so gating manual picks on it left every assistant's dropdown dead in the normal case — and no tooltip could usefully explain that. There is no storm to prevent for a single deliberate click; the election exists to stop several clients writing the same flag on their own initiative. The rank check is not redundant with board permissions: those err **open** when the raid leader doesn't run WhoDoesWhat (`PermissionsOpenReason`), which is exactly when a rank-0 raider reads as fully permitted while the server refuses every `UnitSetRole` they make. `PlayerCanSetGroupRoles` is also a candidacy requirement in `BlizzardRoleWriter` — without it the election picked such a raider and produced a writer who wrote nothing. **Known regression from the merge:** finding three flagged rows in a forty-row bucketed list is worse than working down a three-row filtered window. The overview strip's issue count is the mitigation; sorting flagged rows to the top of each bucket was considered and skipped, since it would break the class-then-name grouping the page is scanned by.
* **Single-role customizer:** full buff priority reorder around an `END` divider (rows below it are banned; Down on the last allowed row bans it, Up on any banned row promotes it), group-role dropdown (Blizzard micro role atlas), icon picker. Four modes — board entry / library custom role / built-in / create. The buff list is editable in all but **built-in**, whose order *is* the default: change one by overriding it for the raid (Custom Roles > Add (+), categories included) or by copying it into a custom role. A built-in role therefore has no Save button at all, no group-role dropdown, and no reorder arrows, with Create a Copy alone in the bottom-right corner. The arrows sit on each row's right edge so hiding them moves nothing; the group-role dropdown sits under the name field (label-less) and pushes the left-aligned buff heading and list down a row when shown, which is the whole difference between `FRAME_H` and `FRAME_H_CUSTOM`. A read-only role states its group role as an icon in front of its name ("[DPS] Fury") instead, so it needs no row. The `Never assigned` divider is grey, small, and hidden whenever the list can't be reordered. The status line above the buttons carries exactly one message — a warning that a board edit affects the whole raid — and is empty in every other mode. Order resolution is board override → custom role's own order → defaults. Taking anything off the board always confirms first, through `ConfirmRemoveRaidRole` (CustomRolesSection.lua), which both the row's `[x]` and the editor's Remove call so the two can't warn about different things. **Custom roles require name + class + group role**; the old "Assign role" opt-out checkbox is gone, so `wowRole` is always one of `dps`/`tank`/`healer`. `ValidateCustomRole` enforces this in Data.lua (not just the editor) and `CreateCustomRole`/`UpdateCustomRole` return nil/false with a printed reason when it fails. Reason: a role with no `wowRole` can't drive the group-role flag, and a main tank switched onto one couldn't be recognised as no-longer-a-tank, so the MAINTANK mark stuck. Legacy custom roles saved with `wowRole=false` are NOT migrated — they open showing DPS and are fixed by saving; the demotion pass below covers them meanwhile.
* **Unit menu (UnitMenuExtensions.lua):** `Menu.ModifyMenu` on self/player/target/party/raid. "Set Role" pop-out (persists, sets role flag, announces); tank-marker grid (multi-select; checking moves the marker onto that tank's row, unchecking clears just the marker — rows are roster-managed), CC target grid, misdirect pop-outs mapping onto stored rows (overwrite covering row or create; one sheep/mage, one tank/hunter). Below a divider: **Non-raider** (`non_raider`, Data.lua) — classless pseudo-role for anyone sitting out; its pseudo-class lives outside `Classes` so Role Preferences never lists it. Non-raiders drop out of buff demand votes, class-filtered rosters (buff/curse dropdowns + pools; `GetEligibleMembers` filters them on any class-filtered query), and the buff grid; marking someone Non-raider clears any paladin-buff rows (and override locks) they held.
* **Main-tank promote (RaidMenuExtensions.lua):** `SetPartyAssignment("MAINTANK",...)` is PROTECTED — can't promote (demote via `ClearPartyAssignment` works, auto for non-tanks). Instead, play the raid-warning sound and bounce an arrow at the player's row after the leader opens Blizzard's Raid tab; `GROUP_ROSTER_UPDATE` cleans up on success. **Multi-tank:** several tanks can be pending at once (`pendingPromotes` set + pooled per-name arrow/glow highlights). Pending entries clear when promoted, no longer tanks, or departed. The addon never calls `ToggleFriendsFrame`, `RaidFrame_LoadUI`, or `HideUIPanel`: current 2.5.6 builds can carry addon taint from an otherwise-allowed out-of-combat panel open into a later native in-combat `RaidFrame:Show`. A lightweight poll notices when the leader opens the Raid tab. Highlights stay parented to `UIParent` and only anchor visually to `RaidGroupButtonN`; reparenting addon frames into that protected hierarchy also risks taint. `StartPromoteWatch` is idempotent per player, and its klaxon is coalesced on a 1s window so a sweep turning up three tanks is one ping, not three overlapping raid warnings. Arrows are raised by **anyone who could actually promote** — `CanPromoteMainTank` (leader or assistant), *not* the flag-writer election: promoting is not a write we coordinate, it's a row we point at for a human to click, and gating it on the single writer meant exactly one person in the raid ever saw the prompt (and nobody at all when the leader doesn't run WDW). Entry points are edge-triggered — a direct tank-role set (`SyncBlizzardRoleState`) or the first-scan sweep in `OnTalentsReady` — so `SweepPromoteWatch` covers the transition neither catches: **our own rank changing**. A `rankWatcher` frame fires it on the false→true edge of `CanPromoteMainTank` only, since `GROUP_ROSTER_UPDATE` fires on every `UnitSetRole` anyone makes and the sweep walks the roster. Without it, an assistant promoted *after* the tanks were assigned had an empty `pendingPromotes` and saw nothing on opening the Raid tab, indefinitely — opening the tab only draws pending entries, it never discovers them.
* **Feral tank detection:** cat DPS and bear tank share the Feral Combat tree, so `SPEC_ROLES` can only guess `druid_feral_dps`. `OnTalentsReady` upgrades the guess to `druid_feral_tank` when the player already reads as a tank — holds the WDW feral-tank role, is flagged `TANK` (`UnitGroupRolesAssigned`), or is a promoted MAINTANK — so a bear that joined pre-flagged as tank isn't detected as DPS and doesn't get its TANK flag stomped.
* **Group messaging:** `WhoDoesWhat:SendGroupMessage(msg)` — RAID/PARTY/silent solo.
* **Editing permissions (Permissions.lua):** raid-only (party/solo always open); rule stands down wholesale (free-assign, gray note says why) in battlegrounds (`IsInInstance()=="pvp"`; sync uses INSTANCE_CHAT there) and when the current leader isn't known to run WDW (`LeaderRunsAddon` checks `syncPeers` — every comm sender Sync hears from, session-only, never wiped; leaderless moments err open; checked live so leadership changes take effect at once); the RAID LEADER picks who edits the board: `leader` / `assistant` (one named player) / `assists` (default — matches the old assist-gating) / `everyone`; leader always may. Setting lives in `db.profile.permissions {mode, assistant}`, rides the sync snapshot, resets to default on group leave. Enforcement is layered: `RequireEditPermission()` backstops every Set\* API (incl. `SetAssignedRole` for others; own role ALWAYS settable — `CanEditRoleOf`), `PruneDepartedAssignments` no-ops read-only, Sync gates broadcasts both ways. `PlayerCanEditAssignments(name)` ranks via `GetRaidRosterInfo`. UI: main view renders read-only without rights — dropdowns swap to plain-text lines (`row.roText`; class colors survive, unlike gray disabled dropdowns), \[+]/\[x]/clear-all/custom-editbox/mail (row + header) hidden, Auto greyed (not hidden) with a permission tooltip that survives the class-disable pass, warnings (except on unassigned rows — a to-do a viewer can't do) + demand + Grid/Info/Calc pop-outs stay, header buttons re-anchor right-aligned around hidden ones (`LayoutHeaderChain`, rightmost-first chains) and text buttons size to their labels; leader gets an "Editing:" picker dropdown top-left (level-2 assistant list via `UIDROPDOWNMENU_MENU_VALUE`), others a gray note naming the rule. Unit menu: pop-outs (tank/CC/MD) require permission, otherwise replaced by gray read-only summary lines of that player's assignments; own Set Role stays live. MembersView dropdowns permission-gated the same way.
* **Assignment sync (Sync.lua, AceAddon module "Sync"):** whole-board sync between WDW users in a group. Syncs roles (`assignments`), tank/CC/misdirect rows, non-paladin static rows (warlock curses), and the permission-gated raid-wide `PallyBuffSource`; **paladin buff rows deliberately excluded for now** — instead providers broadcast their own relevant utility ranks: paladin blessing talents, warlock Improved Healthstone, priest Improved Fortitude, and druid Improved Mark plus Improved Thorns/Brambles (read natively on the sender). A join/reload client's initial HELLO also carries only its three talent-tree totals, letting every WDW peer immediately run the existing class/tab role inference; LibClassicInspector remains the ongoing full-talent source. A fresh direct inspection of another roster member can additionally send a deduplicated `OBSERVE` with the target's three totals and exact relevant utility ranks; any raider may contribute this evidence, receivers validate it against their roster and infer the role themselves, and the leader relays accumulated non-WDW observations to later joiners. Conflict model: leave group → whole board wiped incl. permissions and PallyBuffSource (talent caches survive); join group → a compatible leader is the source of truth (joiner HELLOs, ordinary peers stay silent, leader whispers the full snapshot plus its current peer/version/utility-rank directory, applied unconditionally + popup if it overwrote a non-empty board, 5s timeout keeps local if no compatible leader answers); mid-group any PERMITTED member's edit broadcasts the full snapshot (permissions ride the snapshot; receivers reject STATE from unpermitted senders by their own current belief), last-write-wins with a Lamport rev + sender-name tiebreak so simultaneous edits converge. Unpermitted clients pin their fingerprint each poll (local drift never broadcasts, even after a later promotion) and push own-role changes over a dedicated `ROLE` message everyone accepts (own role only, `lastOwnRoleSent` stops apply-echo); `OBSERVE` is evidence, not a board edit, so it is permission-independent. `PLAYER_ENTERING_WORLD` (+3s; roster unreliable at fire time) re-runs the handshake — members redo the HELLO pull; the leader announces with a STATE broadcast and, once per cold session, requests one group RANKS/VERSION announcement per peer to rebuild the ephemeral directory for everyone — because reload/login wipe `syncPeers` and `GROUP_JOINED` doesn't fire then. Rev is wall-clock-seeded (`max(rev+1, GetServerTime())`): a plain counter reset to 0 on reload and every fresh-session broadcast lost to the old session's higher revs. Change detection = 2s fingerprint poll of the synced slice (write paths are too scattered to hook; view dropdowns mutate entries in place via a local repaint). Applying a remote board updates the fingerprint first so it never echoes back. `/wdw sync` = manual push (leader) / pull (member). `WhoDoesWhat.LOG_SYNC` chat-logs traffic. Protocol versioned (`PROTOCOL`); mismatches warn once and ignore each other.
* **Shared paladin strategy:** `Snapshot()` exposes `db.profile.paladinBuffRules` as `state.paladinStrategy`; it participates in the board fingerprint, leader join replacement, revisions, and permission-gated editing. It is cleared on group leave. `PruneDepartedAssignments()` also removes `prefer` rules whose named paladin left; the existing debounced roster lifecycle elects the leader as the sole housekeeping writer, so views remain read-only renderers and the normal fingerprint poll sends one resulting `STATE`.
* **DB shape (Core.lua):** `assignments` (name→role), `talentSpecs`, `paladinBuffTalents` (+`buffTalentScanVersion`), `warlockHealthstoneTalents`, `coreBuffTalents` (including Gift, Fortitude, and Thorns improvement ranks), `raidAssignments` (row id→name), `paladinBuffRules`, `tankAssignments` (one row per tank: `{player, markers={...}, custom}`; a one-off OnInitialize migration merges the old one-row-per-marker shape by player; Sync's `PROTOCOL` is 9)/`ccAssignments`/`mdAssignments`, `roleCustomizations` (`{buffOrder, allowedCount}`; missing counts are legacy-compatible), `customRoles` (+counter), `permissions` (mode + assistant), `settings`.

* **Addon version discovery:** `Core.lua` reads `WhoDoesWhat.VERSION` from TOC metadata and every Sync message carries it. With a compatible WDW leader, ordinary HELLO receivers stay silent and the leader's initial STATE includes its current peer/version/utility-rank directory. A cold leader reload requests one group RANKS or VERSION announcement per peer to rebuild that ephemeral directory for everyone. Without a compatible leader, stable peers retain the old RANKS/VERSION whisper fallback so presence remains discoverable; clients which are themselves awaiting reload sync stay silent to prevent mutual fanout. The main title shows the standard warning icon when a current group member reports a higher numeric version. In unpackaged source only, Developer → Simulate newer addon version makes that client display, advertise, and compare as the next patch version so a current-version peer sees the out-of-date warning. Every simulator entry point is inside BigWigs `--@do-not-package@` blocks and is removed from release archives.

* **PallyPower talent fallback:** incoming `SELF` reports provide immediate Improved Wisdom/Might ranks and Kings/Sanctuary availability for otherwise-unknown paladins. These tables carry `_source="pallypower"`, are not relayed over WDW sync, and are replaced wholesale (tag removed) by a native scan or authoritative WDW rank message. PallyPower does not report tree totals or spec. Paladins with no native, synced, or external buff-talent data are excluded from WDW's blessing matcher; their rows sort first as `Awaiting talents`, while the leading warning tooltip suggests Rescan or marking someone who is sitting out as Non-raider.

## Releases (CurseForge automatic packaging)
* A GitHub push webhook feeds the CurseForge packager, configured to package **tagged commits only** — pushes to `main` are free, a release happens only when a tag lands.
* To cut a release: commit, then `git tag 1.0.5 && git push origin 1.0.5`.
* Tag text becomes the packaged `## Version:` through `@project-version@`. Use plain `1.0.5`, **no `v` prefix**. A tag containing `beta`/`alpha` (for example `1.1.0-beta`) packages as a Beta/Alpha build instead of a Release.
* Before the release commit, set the TOC debug block's literal `## Version` to exactly the intended tag and add the dated release summary to `Views/AboutView.lua`'s `RELEASES` table. The live main-window title, About window, and peer-version warning read this metadata/history; packaging comments the debug block and substitutes the tag into the earlier `@project-version@` line.
* Keep the version simulator inside `--@do-not-package@` blocks and confirm its setting name is absent from the generated release directory before tagging.
* `.pkgmeta` zips as `WhoDoesWhat/` and strips repo-only files (README.md, AGENTS.md, DEVELOPMENT.md, .vscode, .pkgmeta); `Libs/` ships as committed (no externals).
* `## Interface:` remains literal. CurseForge documents `@project-version@`, but does not document an equivalent token that discovers the current WoW Interface number. Revalidate and update the literal after relevant client patches.
* References: [CurseForge automatic packaging](https://support.curseforge.com/support/solutions/articles/9000197281) and the [BigWigs packager](https://github.com/BigWigsMods/packager).

## Historical client observations (revalidate before relying on them)
* The addon currently targets Interface `20505` for TBC Anniversary. Confirm the current client value after patches; an outdated value can mark the addon out of date.
* Modern Menu API exists: `Menu.ModifyMenu`, `MenuUtil.Create*` + `rootDescription:Insert(desc, index)`. Submenu parents need non-nil click callback.
* `SetPartyAssignment` protected (Blizzard-UI only); `ClearPartyAssignment` is callable. `UnitSetRole` is callable by addons only out of combat as of 2.5.6.
* Raid tab rows are `RaidGroupButton<i>` (i = roster index); `RaidGroup<g>Slot<s>` are empty slots. `Blizzard_RaidUI` is load-on-demand; do not load or open it from addon execution on current builds.
* LibClassicInspector's talent ranks are unreliable on Anniversary (2.5.x): native `GetTalentInfo(tab, i)` index order is unstable, but the lib reads its positional cache assuming static order → ranks land under the wrong talent. Read ranks from native `GetTalentInfo` matched by `(tier, column)` instead (only valid during the lib's inspect or for the local player). NovaRaidCompanion does the same (sorts by row/column).
* `TALENTS_READY` fires only on data arrival, never on roster changes — rejoiners need manual cache replay.
* `UIDropDownMenuTemplate`: ~15px transparent padding/side, label 7px from left / 43px from right — ~69px floor for icon-only box. `StyleDropdown` trims the native 64px housing textures to 57px and moves the housing down 3px for every addon-owned dropdown; it leaves the arrow 0.5px lower and nudges it right 2px, while leaving the text 2.8px lower. Assignment controls also opt into left-aligned labels extended 5px toward the left edge through `SectionKit.LeftAlignDropdown`.
* Scroll children need explicit `SetPoint` or won't render until moved; sibling frames render in unstable order — set explicit `SetFrameLevel` on section boxes.
* Blizzard UI source: github.com/Gethe/wow-ui-source branch `classic_anniversary`.
